> Preserved planning/review record. See the [archive index](../README.md). Statuses, approvals, commands and test results describe their recorded checkpoint; this document is not a new execution authorization.

# G — translation integrity: plan-only assignment

Work only in `worktrees/translation-integrity` on `codex/audit-translation-integrity`, baseline `1bf095d861d921e2378203495bd9a0da0bdf650c`. Read CHARTER and EXECUTION-BOARD, AGENTS, invariant-first-engineering, update-feature-metadata-translations, and sync-docs-with-code. The repository's pending release-translation product specification is context, not authorization to implement its entire roadmap.

First turn: PLAN ONLY. Reproduce current failures with disposable fixtures outside source; inspect all producers/consumers. No tracked edits until root explicitly says PLAN APPROVED. Deliver `plans/translation-integrity.md` with all Gate 1 elements and stop.

Three required local invariants:

1. Localization cannot alter its canonical sidecar, translation source, schema, or existing final output on failure. Reject resolved-path, symlink and hardlink aliases. Write candidate output beside its destination, validate source/output identity and counts, apply fail-on-stale before replacement, then atomically replace. State whether batch locale output is per-file atomic or batch atomic; do not claim the latter without a protocol. Protect report/output aliases too if the CLI can overwrite an input after successful generation. Preserve legitimate empty source semantics explicitly rather than treating every zero count as proof of truncation.
2. Workbook import must associate a translation with its actual source hash/shard, never silently attach positional text when a valid hash disagrees. Validate complete manifest/task/shard identity and exact cardinality before modifying CSV. Cover reordered rows/shards, extra/missing/duplicate/foreign hashes, damaged/blank hashes and headers. Prefer refusing ambiguous damaged workbooks with actionable repair guidance over inventing a positional acceptance workflow. Preserve already completed human translations and source-hash checks. No broad spreadsheet framework or new dependency.
3. Provider failures must remain failed/retryable and cannot count as successful source-provided translations. Default reruns retry only failed current tasks while preserving successful/human work. Genuine source-provided rows remain successful. Define a narrow, evidence-based legacy rule for existing failure rows whose exact producer-written notes say translation failed; never reinterpret all source_provided rows. All callers that choose skip keys (machine and document translation) and materialization/reporting must agree. Keep canonical fallback as canonical metadata, not a fabricated successful translated row. Reports must distinguish valid files, complete translation work and outstanding failures; CLI failure/partial-success behavior must be deliberate.

Inspect `scripts/feature_metadata_localization.py`, `feature_metadata_document_translate.py`, `feature_metadata_machine_translate.py`, relevant callers/tests/skill examples. Baseline findings C4/C5/C6 are in the original contracts review. Cover provider failure then success, repeated failure, source hash changes, human edits, interrupted CSV writes and rerun duplicate-key behavior. A temporary output helper is justified only if it owns shared replace/preserve semantics; do not create a generic workflow engine.

F owns managed remote upload/claim enforcement and exact stale-input/destination generations in `feature_metadata_translation_pipeline.py`. E owns automatic workflow authorization. G initially owns local integrity only. If unmanaged distributed stale overwrite needs a separate correction, describe exact bounded extension for supervisor approval; do not edit F/E overlapping logic on your own.

No Git index/history, commits, pushes, PRs, notifications, remote dataset access/writes, deployments, settings, dependency installations or shared runtime mutations. Use the charter's existing uv runtime with --no-sync and explicit assigned workdir. Deliver baseline failures, final positive/adverse tests, removal pass, precise compatibility and limits, diff check and `reviews/translation-integrity-handoff.md`. Stop for supervisor review; tests alone are not acceptance.
